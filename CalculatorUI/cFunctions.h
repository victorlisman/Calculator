#pragma once

#include <iostream>
#include <vector>

class cFunctions
{
public:
	cFunctions();
	~cFunctions();

public:
	static float power(float a, float b);
	static float root2(float a);
	static float mod(float a);
	//static int16_t log2(uint32_t a);
	//static uint32_t log(uint32_t a, uint32_t b);
};

